import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { Add{{ entity_name }}Component } from './components/add-{{ entity_name.lower() }}/add-{{ entity_name.lower() }}.component';
import { Update{{ entity_name }}Component } from './components/update-{{ entity_name.lower() }}/update-{{ entity_name.lower() }}.component';
import { Delete{{ entity_name }}Component } from './components/delete-{{ entity_name.lower() }}/delete-{{ entity_name.lower() }}.component';
import { List{{ entity_name }}Component } from './components/list-{{ entity_name.lower() }}/list-{{ entity_name.lower() }}.component';

const routes: Routes = [
  { path: 'add-{{ entity_name.lower() }}', component: Add{{ entity_name }}Component },
  { path: 'update-{{ entity_name.lower() }}/:id', component: Update{{ entity_name }}Component },
  { path: 'delete-{{ entity_name.lower() }}/:id', component: Delete{{ entity_name }}Component },
  { path: 'list-{{ entity_name.lower() }}', component: List{{ entity_name }}Component }
];

@NgModule({
  imports: [RouterModule.forRoot(routes)],
  exports: [RouterModule]
})
export class AppRoutingModule { }