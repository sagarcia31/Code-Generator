import React from 'react';
import { BrowserRouter as Router, Route, Switch } from 'react-router-dom';
import Add{{ entity_name }} from './components/Add{{ entity_name }}';
import Update{{ entity_name }} from './components/Update{{ entity_name }}';
import Delete{{ entity_name }} from './components/Delete{{ entity_name }}';
import List{{ entity_name }} from './components/List{{ entity_name }}';

function AppRoutes() {
    return (
        <Router>
            <Switch>
                <Route path="/add-{{ entity_name.lower() }}" component={Add{{ entity_name }}} />
                <Route path="/update-{{ entity_name.lower() }}/:id" component={Update{{ entity_name }}} />
                <Route path="/delete-{{ entity_name.lower() }}/:id" component={Delete{{ entity_name }}} />
                <Route path="/list-{{ entity_name.lower() }}" component={List{{ entity_name }}} />
            </Switch>
        </Router>
    );
}

export default AppRoutes;