import React from 'react';
import { BrowserRouter as Router, Route, Switch } from 'react-router-dom';
import Add{{ entity_name }} from './components/Add{{ entity_name }}';

function AppRoutes() {
    return (
        <Router>
            <Switch>
                <Route path="/add-{{ entity_name.lower() }}" component={Add{{ entity_name }}} />
            </Switch>
        </Router>
    );
}

export default AppRoutes;