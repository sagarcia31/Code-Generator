export async function add{{ entity_name }}(data) {
    const response = await fetch('/api/{{ entity_name.lower() }}', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
    });
    return await response.json();
}